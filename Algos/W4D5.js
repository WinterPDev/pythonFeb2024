/* 
    String Anagrams

    Given a string,
    return array where each element is a string representing a different anagram (a different sequence of the letters in that string).

    Ok to use built in methods
*/

const str1 = "lim";
const expected1 = ["ilm", "iml", "lim", "lmi", "mil", "mli"];
// Order of the output array does not matter

/**
 * Add params if needed for recursion.
 * Generates all anagrams of the given str.
 * - Time: O(?).
 * - Space: O(?).
 * @param {string} str
 * @returns {Array<string>} All anagrams of the given str.
 */

function generateAnagrams(str) {
    // Remember you can add additional parameters as needed! (Hint: We might want to keep passing along any anagrams we've generated.)
    // Consider using slice!
    // Your code here~ 🌻




}

console.log(generateAnagrams(str1)) //["ilm", "iml", "lim", "lmi", "mil", "mli"]; (Order can vary, but that's ok)
/*****************************************************************************/
